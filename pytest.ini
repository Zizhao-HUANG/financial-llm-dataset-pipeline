[pytest]
# This configuration tells pytest to add the project's root directory
# to the Python path before running tests. This ensures that modules
# in the 'src' directory can be imported correctly from the 'tests' directory.
pythonpath = .
